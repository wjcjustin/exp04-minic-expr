4-6 数组的定义和赋值、取值 ng
7 三维数组
11 const array 全局数组
12 带参数的函数声明和调用
26-32 if语句
33-36 while语句
37-38 break-continue
39-41 while-if 综合
42 数组-while 综合
46-47 逻辑运算（与或非、相等、不相等、大于、小于、大于等于、小于等于）
48 while
49-50 逻辑非
51 逻辑运算
后面的都是综合性测试。

在实验四的基础上追加支持分支与循环语句等的识别与翻译。

主要的功能有：

    ok 支持关系表达式。
    ok 支持逻辑与或非表达式，主要要短路求值。
    ok 整型变量可内部转换为布尔类型的值，同样布尔类型的值可转换为整型值；非0的int类型为真，否则为0；真为1，否则为0。
    ok 支持if语句与if-else语句
    ok 支持while语句，不要求do while语句；
    ok 支持break与continue语句；
    ok 分支、循环语句可嵌套————作用域已经实现
    ok 对于分支要定义三个label，真语句体的入口label，假语句体的入口label，分支的出口label；
    ok 对于while循环要定义三个label，循环的入口label，循环体的入口label，循环的出口label；
    ok 对于逻辑与、逻辑或操作，要短路求值，在AST遍历时可创建继承属性真出口label和假出口label；
    ok 对于逻辑非，在AST遍历时可创建继承属性真出口label和假出口label，逻辑非实际上就是真假出口label翻转。
    ok 需要实现ifelse没有大括号的情况


实验七函数定义与调用【必须】

minic编译器支持函数定义与调用，支持的功能主要有：
1）函数定义可支持多个形参；ok

2）函数调用可支持多个实参；ok

3）函数可返回int类型，或void类型；ok

4）支持std.c中定义的各种内置的函数，如putch、getint、getch等函数；

5）在函数的翻译过程中，对于函数定义的形参，对应两个值，一个用来代表实参的值，一个用于局部变量；

6）建议在函数的翻译过程中，如返回int类型，则定义返回值变量，用于保存return语句的返回值，这样可确保函数只有一个出口；

7）对于main函数，建议在函数的开头部分，初始化返回值变量为0，避免main函数的返回值作为进程的退出状态随机值的问题。

todo:
58:22 (!函数)  不识别
119 函数和变量同名的处理 不知道为什么ok了(做完141, 143后, 0c7255a728c2ff0518535e825882914137b474c8)
133 一维数组
141, 143 变量声明的同时定义 ok
